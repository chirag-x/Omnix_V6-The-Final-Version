# Omnix V6 — AI / LLM Foundation
# Phase 1 (capability layer)
# Includes PyTorch + CUDA (cu128) + transformers + openai SDK + chromadb + faiss
#
# Install: pip install -r requirements/ai.txt
# Verified: cp313 + win_amd64 wheels exist for torch 2.11.0+cu128.

# --- PyTorch (CUDA 12.8 build, compatible with driver CUDA 13.2 via forward compat) ---
--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.11.0
torchaudio==2.11.0
torchvision==0.26.0

# --- HuggingFace / transformers ---
# Note: tokenizers version is intentionally not pinned; transformers 5.3.0
# pulls a compatible tokenizers release. Pinning both independently causes
# a ResolutionImpossible error (transformers 5.3.0 wants hf-hub>=1.3.0;
# older tokenizers wanted hf-hub<1.0). Let pip resolve.
transformers==5.3.0
huggingface-hub>=1.3.0,<2.0
safetensors==0.6.2

# --- Embeddings / vector store ---
sentence-transformers==5.2.3
faiss-cpu==1.13.2
chromadb==1.5.2

# --- LLM providers (OpenAI-compatible: OpenRouter, Groq, etc.) ---
openai==2.26.0
httpx==0.28.1

# --- ONNX runtime (UI detection models) ---
onnxruntime==1.24.3

# --- Numeric / ML utilities ---
numpy==2.4.2
scikit-learn==1.8.0

# --- Pillow (image loading, transitive but pinned) ---
Pillow==11.3.0
